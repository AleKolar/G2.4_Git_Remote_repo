# G2.4_Git_Remote_repo

# План выполнения:
1. **Создание и клонирование репозитория**
   - Создайте **публичный (открытый) репозиторий** на [github.com](https://github.com).
   - Склонируйте его на свой ПК через командную строку:

2. **Работа в ветке `main`**
   - Переключитесь на ветку `main` (если ещё не в ней):

   - Создайте файл `file1.txt` с любым содержимым:

   - Добавьте файл в staging и сделайте коммит:

3. **Создание и работа в ветке `develop`**
   - Создайте и переключитесь на ветку `develop`:
     
   - Создайте файл `file2.txt` с любым содержимым:
     
   - Добавьте файл в staging и сделайте коммит:
     
   - Создайте файл `file3.txt` с любым содержимым:
    
   - Добавьте файл в staging и сделайте коммит:

4. **Возвращаемся в ветку `main` и создаём `file4.txt`**
   - Переключитесь обратно на ветку `main`:
    
   - Создайте файл `file4.txt` с любым содержимым:
     
   - Добавьте файл в staging и сделайте коммит:

5. **Слияние ветки `develop` в `main`**
   - Убедитесь, что вы находитесь в ветке `main`:
    
   - Выполните слияние ветки `develop` в `main`:
    
   - Если возникнут конфликты, разрешите их вручную, затем:

6. **Отправка изменений в удалённый репозиторий**
   - Запушите изменения для **обеих веток** (`main` и `develop`):

Сливаем (merge) ветку  'develop' в ветку 'main' используя командную строку и git

Выполняем Push локальных изменений (обеих веток) в удаленный репозиторий на github.com
